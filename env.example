# Google Sheets Configuration
# The spreadsheet ID can be found in the URL of your Google Sheet: https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here

# Service Account credentials (JSON format)
# Copy the entire content of your service-account-key.json file and paste it here
# Handle newlines carefully or paste as a single string
GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", "project_id": "...", "private_key_id": "...", "private_key": "...", "client_email": "...", "client_id": "...", "auth_uri": "...", "token_uri": "...", "auth_provider_x509_cert_url": "...", "client_x509_cert_url": "..."}
# Supabase Configuration
# Create a project at https://supabase.com and get these from Project Settings > API
# Supabase Configuration
# Create a project at https://supabase.com and get these from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Resend Configuration (Email Alerts)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
# The email address to send from (must be a domain you've verified in Resend)
FROM_EMAIL=alerts@yourdomain.com

# URL Configuration
# Used for links in emails (e.g. https://stocktrackr.eu)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cron Configuration
# Protects your alert checking endpoint from unauthorized calls
# Set this to a long random string in production
CRON_SECRET=your_random_secret_here

# PostHog Analytics
# Get these from your PostHog project settings
NEXT_PUBLIC_POSTHOG_KEY=phc_your_posthog_key_here
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com # Or https://app.posthog.com

# Sentry Error Tracking
# Get the DSN from your Sentry project settings -> Client Keys
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
SENTRY_ORG=your_sentry_org_slug_here
SENTRY_PROJECT=your_sentry_project_name_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here # Required for source map uploads in Vercel
